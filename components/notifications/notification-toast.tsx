"use client"

// DISABLED: Toast notifications were causing page blocking issues
// Notifications are still shown in the bell icon dropdown
// This component now does nothing - keeping it for future use if fixed

export function NotificationToastContainer() {
  // Completely disabled - no toast popups
  // Users can view notifications via the bell icon in the header
  return null
}
